[tool.poetry]
name = "netdisco"
version = "1.0.0"
description = "Python code and dependencies for Netdisco"
authors = ["Netdisco Developers"]

[tool.poetry.dependencies]
python = "^3.9"
yamllint = "^1.35.1"
yamale = "^5.2.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
run_worker = "netdisco.runner:main"
